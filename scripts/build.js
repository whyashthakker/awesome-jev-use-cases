import {readFile,writeFile,mkdir,cp,rm} from 'node:fs/promises';
import {scene} from '../shared/visual.js';
import {fileURLToPath} from 'node:url';
import path from 'node:path';
const root=path.dirname(path.dirname(fileURLToPath(import.meta.url)));
const read=p=>readFile(path.join(root,p),'utf8');
const write=(p,s)=>writeFile(path.join(root,p),s);
const catalog=JSON.parse(await read('shared/catalog.json'));
const demos=await Promise.all(catalog.map(d=>read(`use-cases/${d.id}/scenario.json`).then(JSON.parse)));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const repo='https://github.com/whyashthakker/awesome-jev-use-cases';
let base=process.env.SITE_URL || '';
if(base){const u=new URL(base);if(!['https:','http:'].includes(u.protocol))throw new Error('SITE_URL must be an http(s) URL');if(!base.endsWith('/'))base+='/';}
function layout({title,description,body,prefix='',route='',schema}) {
 const url=base?new URL(route,base).href:'';
 return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${esc(title)}</title><meta name="description" content="${esc(description)}"><meta name="theme-color" content="#203a31"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:type" content="website"><meta name="twitter:card" content="summary">${url?`<link rel="canonical" href="${esc(url)}"><meta property="og:url" content="${esc(url)}">`:''}<link rel="icon" href="${prefix}social.svg" type="image/svg+xml"><link rel="stylesheet" href="${prefix}shared/style.css"><script type="application/ld+json">${JSON.stringify(schema).replace(/</g,'\\u003c')}</script><script type="module" src="${prefix}shared/app.js"></script></head>
<body><header><a class="brand" href="${prefix}index.html"><span class="brand-icon">j.</span> jev / use cases</a><nav aria-label="Main navigation"><a href="https://docs.typesafe.ai/introduction">TypeSafe docs ↗</a><a href="${repo}">GitHub ↗</a></nav></header>${body}<footer><span>50 small experiments in structured decisions.</span><span>Community project · Jev by TypeSafe · OpenAI comparison</span></footer></body></html>\n`;
}
const categories=[...new Set(demos.map(d=>d.category))];
const cards=demos.map(d=>`<a class="card" data-category="${esc(d.category)}" href="use-cases/${d.id}/"><div class="card-art">${scene(d,null,d.scenarios[0].state)}</div><div class="card-copy"><div class="card-meta"><span>${d.id.slice(0,2)} / ${esc(d.category)}</span><span>↗</span></div><h2>${esc(d.title)}</h2><p>${esc(d.summary)}</p><div class="tags">${[...new Set(Object.values(d.questions).map(q=>q.type))].map(p=>`<span class="tag">${esc(p)}</span>`).join('')}</div></div></a>`).join('\n');
await write('index.html',layout({title:'50 Jev vs OpenAI Use Cases — Choice, Score & Noul',description:'Explore 50 minimal visual Jev vs OpenAI demos: routing, scoring, moderation, AI judges, traffic and e-learning. Run previews or compare both APIs locally.',schema:{'@context':'https://schema.org','@type':'CollectionPage',name:'50 Jev vs OpenAI visual use cases',description:'A source-linked collection of minimal decision demos, not a performance benchmark.',mainEntity:{'@type':'ItemList',numberOfItems:50,itemListElement:demos.map((d,i)=>({'@type':'ListItem',position:i+1,name:d.title,url:base?new URL(`use-cases/${d.id}/`,base).href:`${repo}/tree/main/use-cases/${d.id}`}))}},body:`<main id="gallery"><section class="hero"><div><span class="pill"><span class="dot"></span> THE STRUCTURED DECISION LAB</span><h1>Less conversation.<br>More decision.</h1><p>50 tiny, visual experiments comparing <strong>Jev</strong> and <strong>OpenAI</strong>. Pick a scenario. Ask a question. Watch the answer become an action.</p><a class="button" href="use-cases/${demos[0].id}/">Start with the three primitives <span>→</span></a></div><div class="hero-visual"><div class="caption"><span>01 / SUPPORT TICKET</span><span>ILLUSTRATIVE</span></div>${scene(demos[0],{decision:{target:'billing',label:'Billing'}},null)}<div class="result-chip"><span>One input. Three questions.</span><span>Choice · Noul · Score</span></div></div></section><div class="intro-line"><span><strong>50</strong> visual use cases</span><span><strong>3</strong> typed primitives</span><span><strong>0</strong> keys needed to preview</span></div><div class="filters"><input id="search" type="search" aria-label="Search use cases" placeholder="Search use cases, primitives, or industries…"><select id="category" aria-label="Filter category"><option value="">All categories</option>${categories.map(c=>`<option>${esc(c)}</option>`).join('')}</select><span class="count" id="count" aria-live="polite">50 / 50 demos</span></div><div class="grid">${cards}</div><p class="empty" id="empty" hidden>No matching demos. Try another search.</p><section class="faq"><h2>What are we comparing?</h2><h3>What is Jev?</h3><p>Jev is TypeSafe’s decision model. It accepts state and typed questions: Choice selects an option, Noul estimates whether a condition is true, and Score places input on an ordered rubric. <a href="https://docs.typesafe.ai/primitives">Read the primitive definitions.</a></p><h3>How is this different from a typical LLM?</h3><p>Jev returns native typed decisions. The OpenAI baseline uses the Responses API with <code>gpt-6-astra</code> and Structured Outputs by default. Both can produce machine-readable results. This collection compares their integration and observed responses; it does not assume a quality, speed or cost winner.</p><h3>Are the previews real model results?</h3><p>No. Previews are hand-authored fixtures shared by both panels. Live runs require your own keys and show measured request duration and provider-reported token usage. One request is not a benchmark.</p><h3>When should I use a generative LLM?</h3><p>Use a generative model when you need writing, explanations, code or multi-step synthesis. Use ordinary code for exact arithmetic, stable experiment assignment and hard constraints. These demos explore the bounded judgments between those two.</p></section></main>`}));
for(let i=0;i<demos.length;i++) {
 const d=demos[i], dir=`use-cases/${d.id}`, prefix='../../';
 const sourceList=d.sources.map(s=>`<li><a href="${esc(s.url)}">${esc(s.title)}</a></li>`).join('');
 const explorer=d.primitiveExplorer?`<div class="primitive-explainer"><article><h2>Choice</h2><p>Pick one known department. Jev returns every option’s probability and a separate confidence.</p></article><article><h2>Noul</h2><p>Does this request a refund? One probability from 0 to 1. Near 0.5 means unsure, not medium intensity.</p></article><article><h2>Score</h2><p>Calm → concerned → very angry. A probability-weighted score can land between levels, from 0 to 2.</p></article></div>`:'';
 const panels=['jev','openai'].map(p=>`<section class="panel" id="${p}" aria-label="${p} result"><div class="panel-head"><h2>${p==='jev'?'Jev':'OpenAI'}</h2><span class="provider-kind">${p==='jev'?'NATIVE TYPED ANSWERS':'RESPONSES API'}</span></div><div class="visual">${scene(d,null,d.scenarios[0].state)}</div><div class="decision" aria-live="polite">Ready when you are</div><div class="metrics">No API call yet</div><div class="answers"></div><p class="result-note"></p><details><summary>Response details</summary><pre>Run to see the response.</pre></details></section>`).join('');
 const body=`<main id="demo" class="demo-main"><a class="back" href="../../index.html">← All 50 use cases</a><div class="demo-heading"><div><span class="eyebrow">${d.id.slice(0,2)} / ${esc(d.category)}</span><h1>${esc(d.title)}</h1></div><span class="pill">${esc(d.pattern)}</span></div><p class="summary">${esc(d.summary)}</p>${explorer}<section class="input-section"><div class="input-top"><label for="state">The same input goes to both models</label><select id="scenario" aria-label="Sample scenario">${d.scenarios.map((s,j)=>`<option value="${j}">${esc(s.name)}</option>`).join('')}</select></div><textarea id="state" aria-label="Scenario state" spellcheck="false">${esc(typeof d.scenarios[0].state==='string'?d.scenarios[0].state:JSON.stringify(d.scenarios[0].state,null,2))}</textarea></section><div class="toolbar"><select id="mode" aria-label="Run mode"><option value="preview">Preview · no API keys</option><option value="live">Live · use my API keys</option></select>${d.primitiveExplorer?'<select id="primitive" aria-label="Primitive"><option value="all">All three together</option><option value="choice">Choice only</option><option value="noul">Noul only</option><option value="score">Score only</option></select>':''}<button class="button" id="run">Run comparison →</button><span id="status" class="status" role="status">Preview uses authored fixtures.</span></div><div class="panels">${panels}</div><details class="options"><summary>Options & setup</summary><label>OpenAI baseline <select id="baseline"><option value="structured">Structured Outputs (default)</option><option value="text">Plain text, JSON prompted</option></select></label><button class="button secondary" id="export" disabled>Export this run</button><p id="key-status">Checking local configuration…</p><p>Add keys to <code>.env</code> on your computer and restart <code>npm start</code>. Keys stay on the local server. Preview mode ignores edits and uses the selected fixture. Live mode sends edited input to both providers.</p></details><div class="notes"><section><h2>Why a typed decision fits</h2><p>${esc(d.why)}</p><h2>Scope of this demo</h2><p>${esc(d.limitation)}</p></section><section><h2>Sources & pattern</h2><ul>${sourceList}<li><a href="https://developers.openai.com/api/docs/guides/structured-outputs">OpenAI Structured Outputs</a></li></ul><p>Sources explain the pattern; they do not establish Jev performance on these original fixtures.</p><a href="${repo}/tree/main/${dir}">View the minimal JavaScript example ↗</a></section></div><details><summary>Typed questions sent to both providers</summary><pre>${esc(JSON.stringify(d.questions,null,2))}</pre></details><nav class="related" aria-label="Adjacent demos">${i?`<a href="../${demos[i-1].id}/">← ${esc(demos[i-1].title)}</a>`:''}${i<49?`<a href="../${demos[i+1].id}/">${esc(demos[i+1].title)} →</a>`:''}</nav><noscript><p>Enable JavaScript to run the interactive comparison. The scenario, questions and sources remain readable above.</p></noscript></main>`;
 await write(`${dir}/index.html`,layout({title:`${d.title} — Jev vs OpenAI Demo`,description:`${d.summary} Compare Jev ${[...new Set(Object.values(d.questions).map(q=>q.type))].join(', ')} with OpenAI in a minimal visual demo.`,prefix,route:dir+'/',body,schema:{'@context':'https://schema.org','@type':'TechArticle',headline:`${d.title}: Jev vs OpenAI`,description:d.summary,codeRepository:repo,about:[{ '@type':'Thing',name:'Jev by TypeSafe'},{'@type':'Thing',name:'OpenAI Responses API'}],citation:d.sources.map(s=>s.url),...(base?{url:new URL(dir+'/',base).href}:{})}}));
 const qtypes=[...new Set(Object.values(d.questions).map(q=>q.type))];
 await write(`${dir}/README.md`,`# ${d.title}: Jev vs OpenAI\n\n${d.summary}\n\n**Primitive:** ${qtypes.join(' + ')} · **Pattern:** ${d.pattern}\n\n## Run\n\nFrom the repository root:\n\n\`\`\`bash\nnpm install\nnpm start\n# Open http://127.0.0.1:3000/${dir}/\n\n# CLI fixture preview, no keys:\nnode --env-file-if-exists=.env ${dir}/run.js\n# Actual provider requests, after setting both keys:\nnode --env-file-if-exists=.env ${dir}/run.js --live\n\`\`\`\n\nUse \`--scenario=1\` for the second input and \`--text\` for the plain-text OpenAI baseline. The browser also supports editable live input and JSON export.\n\n## Why this decision fits\n\n${d.why}\n\nJev receives the state and typed questions directly. OpenAI uses \`client.responses.create({ model: "gpt-6-astra", input, ... })\` with the same state and question definitions, plus a matching JSON schema by default. Both results are validated before the same local decision policy is applied.\n\n## Inputs\n\n${d.scenarios.map(s=>`### ${s.name}\n\n\`\`\`json\n${JSON.stringify(s.state,null,2)}\n\`\`\`\n\nIllustrative values: \`${JSON.stringify(s.expected)}\`.`).join('\n\n')}\n\n## Questions\n\n\`\`\`json\n${JSON.stringify(d.questions,null,2)}\n\`\`\`\n\n## Decision policy\n\n\`\`\`json\n${JSON.stringify(d.policy,null,2)}\n\`\`\`\n\n## What to compare\n\nCompare task agreement on your labeled data, observed request latency, reported token usage, parsing failures, and native Jev distributions where available. Preview fixtures are not model predictions, accuracy labels, or timing measurements. OpenAI's Noul-like values are self-reported estimates; they are not equivalent to Jev's native probabilities.\n\n## Limits\n\n${d.limitation}\n\n## Sources\n\n${d.sources.map(s=>`- [${s.title}](${s.url})`).join('\n')}\n- [OpenAI Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs)\n\nThese sources motivate the pattern; the small scenarios here are original synthetic examples.\n\n[All 50 demos](../../README.md) · [Comparison methodology](../../docs/methodology.md)\n`);
}
await write('llms.txt',`# Awesome Jev Use Cases\n\n> 50 original, minimal visual examples comparing TypeSafe Jev with the OpenAI Responses API.\n\nJev exposes Choice (known category), Score (ordered rubric), and Noul (yes/no probability). This repository compares native typed decisions with generated JSON using gpt-6-astra. Preview data is illustrative; no verified performance ranking is published.\n\n## Documentation\n- [README](${repo}/blob/main/README.md): Setup and full catalog\n- [Methodology](${repo}/blob/main/docs/methodology.md): Fair comparison and limitations\n- [Research](${repo}/blob/main/docs/research.md): Primary sources and selection criteria\n\n## Use cases\n${demos.map(d=>`- [${d.title}](${base?new URL('use-cases/'+d.id+'/',base).href:repo+'/tree/main/use-cases/'+d.id}): ${d.summary}`).join('\n')}\n`);
await write('robots.txt',`User-agent: *\nAllow: /\n${base?`Sitemap: ${new URL('sitemap.xml',base).href}\n`:''}`);
// An absolute sitemap is emitted only when a real publishing URL is supplied.
if(base) await write('sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${['',...demos.map(d=>'use-cases/'+d.id+'/')].map(p=>`<url><loc>${esc(new URL(p,base).href)}</loc></url>`).join('')}</urlset>\n`);
else await rm(path.join(root,'sitemap.xml'),{force:true});
await write('social.svg','<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64"><rect width="64" height="64" rx="14" fill="#203a31"/><text x="16" y="45" fill="#d7edab" font-family="system-ui" font-size="43" font-weight="700">j.</text></svg>\n');
await rm(path.join(root,'dist'),{recursive:true,force:true});await mkdir(path.join(root,'dist/shared'),{recursive:true});
for(const f of ['index.html','llms.txt','robots.txt','social.svg',...(base?['sitemap.xml']:[])])await cp(path.join(root,f),path.join(root,'dist',f));
for(const f of ['app.js','engine.js','visual.js','style.css','catalog.json'])await cp(path.join(root,'shared',f),path.join(root,'dist/shared',f));
for(const d of demos){await mkdir(path.join(root,'dist/use-cases',d.id),{recursive:true});for(const f of ['index.html','scenario.json'])await cp(path.join(root,'use-cases',d.id,f),path.join(root,'dist/use-cases',d.id,f));}
console.log(`Built ${demos.length} demo pages + gallery. Static preview output: dist/${base?' (canonical URLs + sitemap)':' (set SITE_URL for canonical URLs + sitemap)'}`);
